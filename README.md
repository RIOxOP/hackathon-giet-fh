# A project for fraud detection
 
## setup it locally:

### start the server
  ```bash
  cd pyscript
  pip install -r requirements.txt
  python server.py
  ```
### start the ui
  ```bash
  cd ui
  npm install
  npm run dev
  ```
# thank you
